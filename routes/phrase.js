var express = require('express');
var router = express.Router();

var phraseController = require('../controllers/phrase');

//router.post('/getByCodesAndLanguage', phraseController.getByCodesAndLanguage);

module.exports = router;